export default function NotFound() {
    return (
        <div className="fixed inset-0 bg-gray-900 flex items-center justify-center">
            <span>About You não encontrado, <a href="/" className="text-red-500">Crie já o seu {"❤️"}</a></span>
        </div>
    );
}